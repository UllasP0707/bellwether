# Core runtime. Spark and Airflow are deliberately absent: they run in
# containers so this venv stays fast to build and Java-version independent.
pydantic>=2.7,<3
pydantic-settings>=2.3,<3
confluent-kafka>=2.5,<3
psycopg[binary]>=3.2,<4
redis>=5.0,<6
fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
typer>=0.12,<1
rich>=13.7,<15
structlog>=24.1,<26
pyarrow>=17.0
boto3>=1.35,<2
httpx>=0.27,<1
# Intervention copy. Optional at runtime: with no ANTHROPIC_API_KEY set the
# copy desk uses static templates, which is a supported configuration rather
# than a degraded one.
anthropic>=0.40,<1
